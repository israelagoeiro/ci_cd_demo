<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{Titulo}}</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>{{Titulo}}</h1>
        </header>
        
        <main>
            <div class="tarefas-container">
                <h2>Minhas Tarefas XXX</h2>
                
                {{#Tarefas}}
                <div class="tarefa {{#Concluida}}concluida{{/Concluida}}" data-id="{{ID}}">
                    <span class="tarefa-titulo">{{Titulo}}</span>
                    <div class="tarefa-acoes">
                        <span class="tarefa-status">{{#Concluida}}Concluída{{/Concluida}}{{^Concluida}}Pendente{{/Concluida}}</span>
                        <button class="btn-excluir" data-id="{{ID}}" title="Excluir tarefa">X</button>
                    </div>
                </div>
                {{/Tarefas}}
                
                {{^Tarefas}}
                <p class="sem-tarefas">Nenhuma tarefa encontrada.</p>
                {{/Tarefas}}
            </div>
        </main>
        
        <footer>
            <p>CI/CD Demo - Aplicação Go com Fiber e Mustache</p>
        </footer>
    </div>

    <!-- Modal de confirmação -->
    <div id="modal-confirmacao" class="modal">
        <div class="modal-conteudo">
            <h3>Confirmar exclusão</h3>
            <p>Tem certeza que deseja excluir esta tarefa?</p>
            <div class="modal-acoes">
                <button id="btn-confirmar">Sim, excluir</button>
                <button id="btn-cancelar">Cancelar</button>
            </div>
        </div>
    </div>

    <script src="/js/app.js"></script>
</body>
</html> 